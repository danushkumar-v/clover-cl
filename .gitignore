__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
venv/
.env
dist/
build/
*.so
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
data/
inspect_output/
*.log

# internal planning/session files -- kept local, not in the public repo
CLAUDE.md
PLAN.md
handoff.md
SPEC.md

# run artifacts: keep the light ones (csv/npy/json/log), never the weights.
# manifests are regenerable from config_resolved.yaml + seeds (deterministic
# planner), and would add ~85MB of image-index JSON across a full matrix.
*.pt
runs/**/manifest.json
