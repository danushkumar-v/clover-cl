# analysis-only deps (post-processing of runs/ + results_*/ CSVs and .npy
# matrices). No training deps (torch/timm) here. python-pptx is NOT here —
# the slide-deck build is a separate task.
numpy==2.4.6
pandas==3.0.5
PyYAML==6.0.3
matplotlib==3.11.1
seaborn==0.13.2
jupyter==1.1.1
ipykernel==7.3.0
nbformat==5.10.4
nbconvert==7.17.1
